/* 

fscanf(ptr,"%d",&num);      ---used for all type of texts
fgetc(ptr);                 ---used for character by character

fprintf(fptr," %s hello %d ", string , num);     ---used for all type of texts
fputc('c',ptr);             ---used for character by character



 */