//TO GET POWER OPERATOR IN C PROGAMMING YOU NEED:
    // #include<math.h>                         because pow is a operator of math library
    // %f instead of %d in printf function      because math returns a real no.
    // write power as: pow(index, power)

#include<stdio.h>
#include<math.h>

int main()
{
    printf("4^5 is %f", pow(4,5));
    return 0; 

}